{
  "target": "B", "repo": "m0xai_trello-mcp-server", "run": 3,
  "proposed_sites": [
    {"file": "main.py", "line": 6, "snippet": "from mcp.server.fastmcp import FastMCP", "pattern": "1", "reason": "mcp.server.fastmcp module tree no longer exists in v2; must become 'from mcp.server.mcpserver import MCPServer'."},
    {"file": "main.py", "line": 23, "snippet": "mcp = FastMCP(\"Trello MCP Server\")", "pattern": "1", "reason": "Constructs FastMCP(...); the identifier FastMCP no longer exists under that name in v2 even after fixing the import line above, so this line's own text must change to MCPServer(...)."},
    {"file": "server/tools/board.py", "line": 8, "snippet": "from mcp.server.fastmcp import Context", "pattern": "1", "reason": "Context is imported from mcp.server.fastmcp, a module path that no longer exists in v2; must become 'from mcp.server.mcpserver import Context'."},
    {"file": "server/tools/card.py", "line": 8, "snippet": "from mcp.server.fastmcp import Context", "pattern": "1", "reason": "Same broken import path for Context as in board.py; module tree moved to mcp.server.mcpserver."},
    {"file": "server/tools/list.py", "line": 8, "snippet": "from mcp.server.fastmcp import Context", "pattern": "1", "reason": "Same broken import path for Context as in board.py/card.py; module tree moved to mcp.server.mcpserver."}
  ],
  "considered_and_rejected": [
    {"file": "server/tools/board.py", "line": 20, "snippet": "async def get_board(ctx: Context, board_id: str) -> TrelloBoard:", "reason": "Type annotation 'Context' -- once the import on line 8 is fixed to point at mcp.server.mcpserver, this annotation resolves correctly without its own text changing. Same reasoning applies to all other 'ctx: Context' parameter annotations in board.py, card.py, and list.py (not individually listed)."},
    {"file": "server/tools/board.py", "line": 37, "snippet": "        await ctx.error(error_msg)", "reason": "Only a positional message string is passed, no extra= kwarg -- explicitly unaffected per the migration guide's caveat on Context.error/debug/info/warning. Same applies to all other ctx.error(error_msg) calls in board.py, card.py, and list.py."},
    {"file": "server/tools/tools.py", "line": 11, "snippet": "    mcp.add_tool(board.get_board)", "reason": "MCPServer.add_tool() is explicitly called out as unchanged in v2. Same applies to every other mcp.add_tool(...) call in this file."},
    {"file": "main.py", "line": 39, "snippet": "        mcp.run()", "reason": "mcp.run() is not mentioned as renamed/changed in the migration guide; only the FastMCP name itself and the import path changed."},
    {"file": "main.py", "line": 61, "snippet": "                Mount(\"/\", app=mcp.sse_app()),", "reason": "sse_app() is not mentioned as renamed/changed in the migration guide."},
    {"file": "server/utils/trello_api.py", "line": 21, "snippet": "        self.client = httpx.AsyncClient(base_url=self.base_url)", "reason": "This httpx.AsyncClient is used for the application's own calls to the third-party Trello REST API, not passed into any MCP SDK function; per the guide's explicit caveat, unrelated application httpx usage is not affected by the SDK's internal httpx->httpx2 transport change."},
    {"file": "server/models.py", "line": 13, "snippet": "    idOrganization: str | None = None", "reason": "This is a custom Pydantic model field mirroring Trello's own REST API JSON field names, not an attribute of an MCP SDK protocol object (e.g. Tool, CallToolResult). Pattern 2 only applies to MCP SDK protocol objects; camelCase fields like idOrganization/idBoard/idList/idMembers/idLabels/dueComplete throughout models.py and the dtos/*.py files are unaffected for the same reason and are not individually listed."}
  ]
}
