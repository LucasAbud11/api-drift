"""Third-party integrations used by OpsMesh tools (currently just GitHub).

These are plain HTTP clients unrelated to the MCP SDK - kept in their
own package so tool modules don't need to know about `httpx` directly.
"""
